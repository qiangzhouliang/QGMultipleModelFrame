server:
  port: ${PORT:30101}
  servlet:
    context-path: /ucenter
spring:
  application:
    name: service-ucenter
  profiles:
    active: common,eureka,ribbon,mybatis_pageHelper,mysql  # 引入公共插件
  main:
    allow-bean-definition-overriding: true

